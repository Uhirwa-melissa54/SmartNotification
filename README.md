# Smart Notification System (Spring Boot + WebSocket)

A real-time notification system built using **Spring Boot**, **WebSockets**, and **STOMP**.  
This project allows the backend to push instant notifications to connected clients without page refresh.

---

## 🚀 Features

- Real-time communication using WebSocket + STOMP
- Publish/subscribe messaging model
- Backend can send notifications to:
  - All connected users
  - Specific users (private messaging)
- Simple REST API to trigger notifications
- Lightweight architecture suitable for dashboards, alert systems, admin panels, etc.

---



